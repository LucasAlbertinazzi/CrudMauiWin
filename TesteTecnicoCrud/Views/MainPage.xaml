<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="TesteTecnicoCrud.Views.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Clientes">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="CaptionStyle" TargetType="Label">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="TextColor" Value="#9AA0A6" />
            </Style>

            <Style x:Key="ValueStyle" TargetType="Label">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="TextColor" Value="White" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <VerticalStackLayout Padding="16" Spacing="12">
        <HorizontalStackLayout Spacing="8">
            <Button Command="{Binding AddCommand}" Text="Novo" />
            <Button Command="{Binding EditCommand}" Text="Editar" />
            <Button Command="{Binding DeleteCommand}" Text="Excluir" />
        </HorizontalStackLayout>

        <CollectionView
            Margin="0,8,0,0"
            ItemsSource="{Binding Clientes}"
            SelectedItem="{Binding SelectedCliente, Mode=TwoWay}"
            SelectionMode="Single">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border
                        Margin="0,6"
                        Padding="12"
                        BackgroundColor="{AppThemeBinding Light='#2A2A2A',
                                                          Dark='#1F1F1F'}"
                        Stroke="#333"
                        StrokeThickness="1">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="10" />
                        </Border.StrokeShape>

                        <Grid
                            ColumnDefinitions="Auto,*"
                            ColumnSpacing="8"
                            RowDefinitions="Auto,Auto,Auto,Auto"
                            RowSpacing="4">

                            <Label
                                Grid.Row="0"
                                Style="{StaticResource CaptionStyle}"
                                Text="Nome:" />
                            <Label
                                Grid.Row="0"
                                Grid.Column="1"
                                Style="{StaticResource ValueStyle}"
                                Text="{Binding Name}" />

                            <Label
                                Grid.Row="1"
                                Style="{StaticResource CaptionStyle}"
                                Text="Sobrenome:" />
                            <Label
                                Grid.Row="1"
                                Grid.Column="1"
                                Style="{StaticResource ValueStyle}"
                                Text="{Binding Lastname}" />

                            <Label
                                Grid.Row="2"
                                Style="{StaticResource CaptionStyle}"
                                Text="Idade:" />
                            <Label
                                Grid.Row="2"
                                Grid.Column="1"
                                Style="{StaticResource ValueStyle}"
                                Text="{Binding Age}" />
                            <Label
                                Grid.Row="3"
                                Style="{StaticResource CaptionStyle}"
                                Text="Endereço:" />
                            <Label
                                Grid.Row="3"
                                Grid.Column="1"
                                LineBreakMode="TailTruncation"
                                Style="{StaticResource ValueStyle}"
                                Text="{Binding Address}" />
                        </Grid>

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="Selected">
                                    <VisualState.Setters>
                                        <Setter Property="Stroke" Value="#7C4DFF" />
                                        <Setter Property="StrokeThickness" Value="2" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
